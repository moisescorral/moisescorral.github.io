(function(){'use strict';const $=(s,p=document)=>p.querySelector(s),$$=(s,p=document)=>[...p.querySelectorAll(s)];const head=$('#siteHead'),bar=$('#progressBar');function scrollState(){const y=scrollY,max=document.documentElement.scrollHeight-innerHeight;head.classList.toggle('compact',y>70);bar.style.width=(max?y/max*100:0)+'%'}addEventListener('scroll',scrollState,{passive:true});scrollState();const observer=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)e.target.classList.add('visible')}),{threshold:.1});$$('.reveal').forEach(e=>observer.observe(e));const button=$('#menuButton'),menu=$('#mobileMenu');function setMenu(open){menu.classList.toggle('open',open);menu.setAttribute('aria-hidden',String(!open));button.setAttribute('aria-expanded',String(open))}button.addEventListener('click',()=>setMenu(!menu.classList.contains('open')));$$('a',menu).forEach(a=>a.addEventListener('click',()=>setMenu(false)));$('#readButton').addEventListener('click',e=>{const reading=$('#manuscript').classList.toggle('reading');e.currentTarget.textContent=reading?'Cerrar modo lectura':'Modo lectura'});})();
